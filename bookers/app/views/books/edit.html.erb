<% if @book.errors.any? %>
<%= @book.errors.count %>errors prohibited this book from being saved
<ul>
<% @book.errors.full_messages.each do |message| %>
<li><%= message %></li>
<% end %>
</ul>
<% end %>

<%= form_with model: @book do |f| %>

<div>
  <%= flash[:notice] %>
</div>

<h3>Editing Book</h3>
  <p>Title</p>
  <%= f.text_field :title %>
  
  <p>Body</p>
  <%= f.text_area :body %>
  </br>
  <%= f.submit 'Update Book' %>
  </br>

  <%= link_to "Show", book_path(@book.id) %> | <a href="/books">Back</a> </p>
  

<% end %>